# File: backend/.env.example

# Copy this file to .env and fill in your actual values.
# DO NOT COMMIT your .env file to version control.

# Required: Your Sentry Internal Integration Token (ensure necessary permissions)
SENTRY_API_TOKEN="your_actual_sentry_api_token_here"

# Optional: Override Sentry API base URL if using self-hosted Sentry
# SENTRY_BASE_URL="https://your-sentry-instance.com/api/0/"

# Optional: Override Sentry Web UI base URL (used for generating links)
# Default assumes SaaS structure like https://sentry.io/organizations/<org>/...
# For self-hosted, might be just "https://your-sentry-instance.com/"
# SENTRY_WEB_URL="https://sentry.io/"

# Optional: Override Ollama URL if it's not running on the default port/host
# OLLAMA_BASE_URL="http://your-ollama-host:11434"

# Optional: Specify the default Ollama model to use (ensure it's pulled)
# OLLAMA_MODEL="llama3:latest"

# Optional: Timeout in seconds for Ollama API requests (default: 1200)
# Adjust this value based on your hardware capabilities
# For development on slower machines, a higher value (20 minutes = 1200 seconds) is recommended
# For production or powerful machines, you can lower it (e.g., 120-300 seconds)
# OLLAMA_TIMEOUT=1200

# Optional: Set log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL="INFO"