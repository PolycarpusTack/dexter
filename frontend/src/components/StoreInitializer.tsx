/**
 * Store Initializer Component
 * Migration from appStore to individual stores has been completed.
 * This component is kept for future store initialization needs.
 */
export function StoreInitializer({ children }: { children: React.ReactNode }) {
  // Migration has been completed - appStore no longer exists
  // This component can be used for future store initialization needs
  return <>{children}</>;
}