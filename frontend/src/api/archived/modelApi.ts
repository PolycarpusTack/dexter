/**
 * @deprecated This file is archived. Import from 'api/unified/aiApi' instead.
 * See the aiApi.ts and related hooks in the unified directory.
 */

// Re-export from unified for backward compatibility
import { 
  fetchModels,
  getModelDetails,
  setActiveModel
} from '../unified/aiApi';

export {
  fetchModels,
  getModelDetails,
  setActiveModel
};